DB_HOST="{{ db_host }}"
DB_NAME="{{ db_name }}"
DB_USER="{{ db_user }}"
DB_PASSWORD="{{ db_password }}"
DB_PORT="{{ db_port }}"
AWS_REGION="{{ aws_region }}"
S3_BUCKET_NAME="{{ s3_bucket_name }}"
WORKER_URL="http://{{ hostvars[groups['worker'][0]]['ansible_host'] }}:6000"
