# Local development only.
# The Kubernetes deployment does not read this file: it takes BACKEND_URL from
# the ConfigMap in k8s/20-configmap.yaml — a Kubernetes Service DNS name, not
# an IP — and SECRET_KEY from a Kubernetes Secret.

BACKEND_URL=http://your-backend-private-ip:5000
SECRET_KEY=your-secret-key-here
