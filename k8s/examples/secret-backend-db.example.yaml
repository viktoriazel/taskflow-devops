# Example only — placeholder values, not a real Secret.
# The real object must be created out-of-band in Phase 11 (never committed
# to Git, sourced from the existing AWS Secrets Manager RDS-managed secret
# via an operator-mediated transfer) and must use the name below without
# the "-example" suffix:
#   metadata.name: taskflow-db-credentials
apiVersion: v1
kind: Secret
metadata:
  name: taskflow-db-credentials-example
  namespace: devops-app
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: taskflow
type: Opaque
stringData:
  DB_USER: "REPLACE_WITH_RDS_MASTER_USERNAME"
  DB_PASSWORD: "REPLACE_WITH_VALUE_FROM_AWS_SECRETS_MANAGER"
