# Example only — placeholder values, not a real Secret.
# The real object must be created before deploying the application (never
# committed to Git) and must use the name below without the "-example" suffix:
#   metadata.name: taskflow-frontend-secret
# See the README section "Create the Kubernetes Secrets" for the procedure.
apiVersion: v1
kind: Secret
metadata:
  name: taskflow-frontend-secret-example
  namespace: devops-app
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: taskflow
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_WITH_GENERATED_SECRET_KEY"
